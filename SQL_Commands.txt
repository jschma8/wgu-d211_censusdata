download file 
extract all
destination location needs to be "C:\Users\Public"

Open pgAdmin4
In Browser, Open Server > Databases > Churn
right click on Churn and open Query Tool
In query editor copy and paste everything below:

-- SQL Commands: -- 
-- create the table census -- 
create table census
(Geographic_Area_Name varchar,
"State" varchar,
"2017_NAICS_code" varchar,
NAICS2017_LABEL varchar,
SEX_LABEL varchar,
ETH_GROUP_LABEL varchar,
RACE_GROUP_LABEL varchar,
VET_GROUP_LABEL varchar,
Number_of_employer_firms varchar,
Number_of_employees varchar,
"Annual_payroll_$1,000" varchar
);

-- import data --
COPY census
FROM 'C:\Users\Public\wgu-d211_censusdata-main\ABSCS2020.AB2000CSA01-Data.csv'
DELIMITER ','
CSV HEADER;

-- clean up data --
-- drop to column --
alter table census
drop column geographic_area_name;

--- drop rows ---
DELETE from census where not "2017_NAICS_code" <> '0';
DELETE from census where eth_group_label <> 'Total';
DELETE from census where race_group_label <> 'Total';
DELETE from census where vet_group_label <> 'Total';
DELETE from census where sex_label = 'Total';
DELETE from census where sex_group_label = 'Equally male/female';

alter table census
drop column female_employees;

ALTER TABLE census
add female_employees varchar;

--UPDATE employees, emp2
--SET employees.first_name = emp2.first_name
--WHERE employees.id = emp2.id

UPDATE census
SET female_employees = number_of_employees
Where sex_label = 'Female';

--ALTER TABLE census
--add female_payroll varchar;

--ALTER TABLE census
--add male_payroll varchar;

--ALTER TABLE census
--add male_payroll varchar;

select * from census;
